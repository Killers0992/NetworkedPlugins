namespace NetworkedPlugins.API.Enums
{
    public enum AddonSendPermissionTypes : byte
    {
        None,
        Everything,
        PlayerNickname,
        PlayerRole,
        PlayerDoNotTrack,
        PlayerRemoteAdminAccess,
        PlayerOverwatch,
        PlayerIPAddress,
        PlayerMute,
        PlayerIntercomMute,
        PlayerGodmode,
        PlayerHealth,
        PlayerMaxHealth,
        PlayerGroupName,
        PlayerRankColor,
        PlayerRankName,
        PlayerPlayerID,
        PlayerPosition,
        PlayerRotation
    }
}
